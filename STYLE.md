# 문체 규칙

이 책 본문(`chapters/**/*.qmd`)의 한국어 문장을 다듬는 기준이다.
`scripts/prose_audit.py` 가 후보를 찾고, 고칠지 말지는 아래 기준으로 판단한다.

이 문서가 다루는 것은 **문장**이다. 내용·설명 순서·수식·용어 선택은 대상이 아니다.
용어 표기는 억지로 한글로 옮기지 않는다(norm, bootstrap, p-value 등 원어 유지).

---

## 먼저: 고치지 않는 것

탐지기가 찾은 것의 절반 이상은 멀쩡한 문장이다. **기계적으로 다 고치면 글이 나빠진다.**

다음은 그대로 둔다.

- **짧고 대구를 이루는 정의문.**
  "inf와 sup의 순서를 뒤집은 것이 **쌍대 문제**다. 안쪽 하한을 먼저 취한 것이 **쌍대 함수**다."
  → 둘이 짝을 이뤄 리듬이 산다. 손대지 않는다.
- **관용이 된 표현.** "남는 것이 없다", "그게 전부다", "그뿐이다".
- **문단 첫머리의 접속부사 한 번.** "그래서 결과가 나빠지는 경우가 둘로 갈린다."
  → 흐름을 이어주는 접속은 한국어 글쓰기에서 정상이다.
  연속 세 문단이 모두 "그리고/그래서"로 시작할 때만 손본다.
- **의도적인 체언 종결.** "여기서 결정적인 사실 하나."
- **강조를 위한 짧은 반복.** "값이 아니라 값의 변화율, 즉 **곡률**이다."

판단 기준은 하나다 — **소리 내어 읽어서 걸리는가.** 걸리지 않으면 두고 넘어간다.

---

## 고치는 다섯 가지

### 1. 영어 구문 직역

`이유는 ~에 있다`(lies in), `~이 X를 Y로 만든다`(makes X Y), `~에 대한 답`(answer to).

```
❌ 코드가 벌점형을 푸는 이유는 계산에 있다.
✅ 코드가 벌점형을 푸는 것은 순전히 계산이 편해서다.

❌ @ 이라는 사실이 reverse-mode를 유일하게 실행 가능한 선택지로 만든다.
✅ 파라미터 공간의 @ 은 수십억인데 손실은 스칼라라 @ 이다.
   그래서 실행 가능한 선택지가 reverse-mode 하나뿐이다.
```

### 2. 주어와 술어가 어긋난 문장

```
❌ 위 연쇄법칙 식에 @ 이 등장하는 이유는 성분으로 쓰면 한 줄이다.
✅ 위 연쇄법칙 식에 @ 이 등장하는 이유는 성분으로 써 보면 한 줄로 드러난다.

❌ 신경망에서 자주 만나는 세 층의 야코비안은 계산해 두면 계속 쓴다.
✅ 신경망에서 자주 만나는 세 층의 야코비안은 한 번 계산해 두면 두고두고 쓰인다.
```

### 3. 관형절 과적재 — 결론이 뒤로 밀린 문장

영어는 관계절을 명사 뒤에 붙이지만 한국어는 앞에 쌓인다.
수식어가 20자를 넘으면 **결론을 앞으로 빼고 설명을 뒤로** 돌린다.

```
❌ 곱수 @ 는 **그 제약을 한 단위 느슨하게 했을 때 최적값이 얼마나 좋아지는가**를
   재는 그림자 가격이다.
✅ 곱수 @ 는 제약에 매겨진 **그림자 가격**이다 — 그 제약을 한 단위 느슨하게 하면
   최적값이 그만큼 좋아진다.
```

### 4. 지시어를 영어 it / this 처럼 쓴 자리

한국어 "이것"은 영어 it보다 훨씬 무겁다. **가리키는 명사를 되살린다.**

```
❌ @ 이라는 부호 제약이 전체 장치의 핵심이다. 이것 덕분에 …
✅ @ 이라는 부호 제약이 전체 장치의 핵심이다. 이 부호 덕분에 …

❌ 원 문제가 아무리 험해도 쌍대 문제가 볼록이 되는 근거가 이것뿐이다.
✅ 원 문제가 아무리 험해도 쌍대 문제는 볼록이 된다. 그 근거가 이 한 줄뿐이다.
```

### 5. 같은 명사·구문의 반복

```
❌ @ 를 튜닝하는 일은 사실 **복잡도 예산 @ 를 간접적으로 정하는 일**이다.
✅ @ 를 튜닝한다는 것은 사실 **복잡도 예산 @ 를 에둘러 정하는 셈이다.**

❌ **볼록함수**는 두 점을 잇는 선분이 함수 위에 있는 함수다.
✅ **볼록함수**는 그래프 위의 두 점을 이은 선분이 언제나 그래프보다 위에 놓이는 함수다.
```

---

## 작업 절차

```bash
python scripts/prose_audit.py                    # 파트별 후보 수
python scripts/prose_audit.py --chapter          # 후보가 많은 챕터 순
python scripts/prose_audit.py chapters/00_foundations/00_linear_algebra.qmd
                                                 # 그 챕터의 후보 문단
```

챕터 하나를 끝낼 때마다:

1. 후보 문단을 읽고 "소리 내어 읽어 걸리는" 것만 고른다
2. 치환은 `assert` 로 보호한 스크립트로 적용한다 (원문과 정확히 일치할 때만 바뀌게)
3. 파트 하나가 끝나면 커밋 1개

수정 중 **내용이 바뀌면 안 된다.** 문장이 어색해서 고치는 것이지
설명이 틀려서 고치는 것이 아니다. 사실 오류를 발견하면 따로 알리고 별도로 다룬다.

---

## 1차 전수 점검 결과 (2026-09-10)

123개 챕터를 전부 통독하고 **24곳**을 고쳤다. 파트별로 이렇게 갈렸다.

| 파트 | 수정 | 파트 | 수정 |
|------|-----:|------|-----:|
| 0 기초 | 38 | 7 언어 | 3 |
| 1 데이터 사이언스 | 5 | 8 오디오 | 3 |
| 2 고전 ML | 5 | 9 비전·비디오 | 1 |
| 3~4 딥러닝·아키텍처 | 2 | 10~13 | 0 |
| 5 트랜스포머 | 2 | | |

**0.1 한 챕터가 38곳 중 28곳을 차지한다.** 나머지 122개 챕터를 합쳐도
14곳뿐이다. 즉 이 책의 문장은 전반적으로 상태가 좋고, 손댈 곳은
특정 챕터에 몰려 있다.

문체와 별개로 발견해 함께 고친 실제 결함이 있었다.

- **한자 혼입 4곳** — `코드 補完`, `1步`. 유니코드 예시의 `日本語` 는 의도된 것이라 유지.
- **등호 구분선 4곳** — 본문에 `=` 를 70여 개 늘어놓은 줄. 마크다운에서
  구분선이 아니라 문단이라 빌드 결과에 등호가 그대로 찍혀 나왔다.
- **0.2 문법 오류** — "분산이 독립을 요구한다는 점이 자주 놓친다"
- **0.1 야코비안 표** — 층이 셋인데 본문은 "두 층"

**존댓말/해라체는 관례가 있다.** 블록 종류별로 세어 확인했다.

| 블록 | 해라체 | 존댓말 |
|------|-------:|-------:|
| key-concept | 99 | 0 |
| callout-note / tip / important | 63 | 0 |
| callout-warning | 52 | 7 |
| interaction-hint | 13 | 111 |

**그림에 대해 독자에게 말할 때만 존댓말**이다 — interaction-hint 전부와,
"이 그림은 개념 설명용입니다" 류의 면책 callout-warning 7개가 그렇다.
나머지는 해라체다. 이 경계를 넘는 혼용 2곳(2.1, 5.5)만 고쳤다.

---

## 조사 띄어쓰기 (2026-09-10 확정)

**로마자·숫자 뒤 조사는 붙여 쓴다.** `Ridge는`, `LoRA의`, `24%가`.
1,240곳을 일괄 변환했고, 남은 띄어쓰기는 0곳이다.

**수식 뒤는 띄어 쓴다.** `$t$ 를`, `$\lambda$ 가`.
수식은 별도의 조판 단위라 붙이면 오히려 읽기 나쁘다.
`scripts/normalize_josa.py` 가 수식·인라인 코드·링크 URL·코드 청크를 마스킹해 보호한다.

```bash
python scripts/normalize_josa.py --dry    # 무엇이 바뀌는지만 출력
python scripts/normalize_josa.py          # 적용
```

### bold 뒤에 조사를 붙일 때 깨지는 경우

**bold 가 문장부호로 끝나면 조사를 붙일 수 없다.**

```
✗ **24%**로          ✗ **오토인코더(SAE)**는       ✗ **'문제'**로
✓ **24%로**          ✓ **오토인코더**(SAE)는       ✓ **'문제'로**
```

Pandoc(= 이 책의 빌드)은 이것을 통과시키지만
**CommonMark(VSCode 미리보기·GitHub)는 bold 를 열지 못하고 `**` 를 글자로 뱉는다.**
닫는 `**` 앞이 문장부호이고 뒤가 한글이면 "right-flanking" 조건이 깨지기 때문이다.
bold 가 **한글이나 로마자로 끝나면** 붙여도 안전하다.

더 위험한 것은 **여는 `**` 뒤의 공백**(`** 굵게**`)이다.
이쪽은 양쪽 렌더러 모두에서 실패하고, **그 뒤 문단 전체가 함께 망가진다.**

두 경우를 한 번에 잡는 검사기를 두었다.

```bash
node scripts/check_bold.js    # 문단 단위로 CommonMark 파싱, 깨진 곳만 출력
```

전수 검사 결과 **3,786개 문단 중 깨진 곳 0개**다.

## 설명은 print() 밖에, 수치는 인라인 코드로 (2026-09-15 확정, Part 1 적용)

`print()` 안에 쓴 마크다운은 코드 출력에 그대로 찍힌다 — `**굵게**` 는 별표로,
`[1.0](00_data_and_types.qmd)` 는 텍스트로, 수식은 아예 못 쓴다. 설명 문장을
print 로 찍으면 독자에게는 "설명" 이 아니라 "로그" 로 읽힌다.

**규칙**

1. 코드 청크의 `print()` 에는 **계산 결과**만 남긴다 — 숫자 표, 실측값, 판정.
2. 해석·권고·정의·비교 문장은 청크 **밖** 마크다운 본문에 쓴다. 강조·링크·수식·목록이 살아난다.
3. 계산 없는 표(방법 비교, 규칙 목록 등)는 `print` 로 찍지 말고 마크다운 표로.
4. 본문에서 계산값을 인용할 때는 숫자를 손으로 적지 말고 **Quarto 인라인 코드**를 쓴다.

   ```
   평균이 `{python} f"{base_mean:.2f}"` 에서 `{python} f"{best_mean:.2f}"` 로
   ```

   - 항상 f-string 으로 감싼다. `{python} x.max()` 처럼 넘기면 `np.float64(3.5)` 가 새어 나온다.
   - 인용할 값은 청크 안에서 `float(...)` 로 변수에 담아 둔다. 긴 식을 인라인에 넣지 않는다.
   - 요약표의 숫자도 같은 방식으로. Part 1 점검에서 하드코딩된 요약 수치 3곳이
     실제 출력과 달랐다(t(3) 첨도 79.7 vs 실측 27.7 등).
5. 판정 문자열(`"약한 도구"`, `"반드시 통제"`)에도 `**` 를 넣지 않는다 — 출력에 찍힌다.

**주의**

- 인라인 코드 안에서 `$...$` 수식과 붙여 쓸 때 `$|z| = $ \`{python} ...\`` 처럼 닫는 `$` 앞에
  공백이 들어가면 수식이 깨진다. `$|z|$ 가 \`{python} ...\` 로` 처럼 수식을 먼저 닫는다.
- 목록 표시 없이 줄만 바꾼 텍스트는 한 문단으로 합쳐진다. 항목은 `-` 나 표로.
- 인라인 코드를 **줄 첫머리**에 두지 않는다. 출력이 `79)` 나 `3.` 처럼 나오면 마크다운이
  번호 목록으로 읽는다(4.3 에서 실제로 발생). 줄을 나눌 때는 인라인 앞에 단어를 하나 둔다.
- 인라인 코드의 출력은 마크다운으로 다시 해석되지 않는다. `$b_f$`, `**` 같은 표기를 출력
  문자열에 넣지 말고 본문 쪽에 둔다.

## 풀어쓰기 전수 재작성 (2026-09-17 시작)

독자 피드백: 한자어로 축약 번역된 용어(단위구, 노름, 등고선, 발산, 허용변환 …)가 읽기를 막고,
"다 안다"는 식으로 생략된 설명이 서문의 목표("고등학교 수학만 알면 이해")와 맞지 않는다.
수식·예시만으로는 부족했다 — 일상 비유가 필요하다.

**규칙**

1. **전문 용어는 영문 원어를 주 표기로.** 첫 등장에 한 줄 풀이를 붙인다.
   고등학교 수학 용어(행렬, 벡터, 확률, 평균, 분산, 미분, 기울기, 로그, 함수, 극한)는 한국어 유지.
   영문으로 바꾸는 것의 예: unit sphere, eigenvalue/eigenvector, SVD, low-rank, sparse,
   condition number, regularization, KL divergence, self-information, dual, KKT, Jacobian,
   Frobenius norm, broadcasting, false positive, effect size.
2. **생략하지 않는다.** 한 줄 결론문("X 는 Y 다")은 왜 그런지가 뒤따라야 한다. 길어져도 된다.
3. **일상 비유를 붙인다.** 새 개념마다 최소 한 개.
4. 요약표도 같은 기준으로 고친다.

**진행**: Part 0 부터 순서대로. 챕터당 커밋 1개.

**로마자 뒤 조사 형태.** 붙여 쓰되(위 "조사 띄어쓰기" 절), 이/가·은/는·을/를·과/와는
한국어로 읽었을 때의 끝소리로 고른다. `gradient descent가`, `Jacobian이`, `L1은`, `FDR은`, `logit이`, `CLIP이`.
`python scripts/normalize_josa.py` 로 띄어쓰기를 붙인 뒤 세션 스크립트 `josa.py` 로 형태를 맞췄다.

**진행 기록**: Part 0 (0.0–0.5) 완료 2026-09-17. 다음은 Part 1.
